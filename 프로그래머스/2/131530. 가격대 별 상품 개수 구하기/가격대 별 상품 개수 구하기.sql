SELECT
(CASE
WHEN TRUNC(PRICE/10000)>=0
THEN TRUNC(PRICE/10000)*10000
END) AS PRICE_GROUP,
COUNT(PRODUCT_ID) AS PRODUCTS
FROM
PRODUCT
GROUP BY
(CASE
WHEN TRUNC(PRICE/10000)>=0
THEN TRUNC(PRICE/10000)*10000
END)
ORDER BY PRICE_GROUP ASC